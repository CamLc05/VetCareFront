<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Veterinaria.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Veterinaria.Views"
    Shell.FlyoutBehavior="Disabled"
  
     >
    
    <!-- Shell.FlyoutBehavior="Flyout" -->

    
    
    <TabBar>
        <ShellContent Title="Pacientes" ContentTemplate="{DataTemplate local:PacientesPages}"/>
      <!--  <ShellContent Title="Login" Icon="citas.png" ContentTemplate="{DataTemplate local:LoginPage}" />
      <ShellContent Title="Perfil" Icon="perfil.png" ContentTemplate="{DataTemplate views:ProfilePage}" />  -->
    </TabBar>
   <!-- <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />  -->

</Shell>
